#!/bin/sh
# Select the active checkout's development check. Qualification is explicit and hosted once.
set -eu
ROOT="$(git rev-parse --show-toplevel)"
exec node "$ROOT/scripts/development-push-check.mjs"
