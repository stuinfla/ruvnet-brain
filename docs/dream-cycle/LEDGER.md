| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-19 | memory-durability | `restore-local-ingests.mjs` could not distinguish "store root never materialized on this host" from "wiped" — a fresh checkout, CI runner, or this nightly agent's own container all logged an identical false-wipe alarm | #142 | #143 | yes | ACCEPT | 7 new unit tests, red→green; `test:integration` 37 files reproduced identically vs unmodified `main`; both-hosts conformance green. Baseline exit 1 / wipe-wording → candidate exit 2 / honest "NOT evidence of a wipe" wording, on this container's real state | 275223bea7234cd4ebef5182083ea627d9aba459828248bc9f1398f62787d019 | night one — ledger was empty, nothing to re-check |
| 2026-08-20 | cross-host-conformance | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=0/DEEP) landed first as #147/#148 (test-harness fix, vacuous Codex TEETH assertions); this row is this session's independent, non-overlapping finding: `hook-registry.mjs`'s own drift-detection mesh census never enumerated `plugin/hooks/codex-hooks.json`, so M1/M3/M5/M6 had zero automated coverage of the Codex host | #149 | #150 | yes | ACCEPT | Codex layer added, all 16 registrations resolve via the existing shim table (zero invented contracts), M1/M3/M5/M6 clean; 2 downstream consumer regressions found+fixed (`dispatchGateWiring()`, `checkCoexistence()`). `test:integration` candidate's 9 failures a strict subset of baseline's 10, byte-identical overlap | b3822a6584510d770d107a52884752399eb83c5352d05dff7dff8e7ffcfffe47 | #142/#143 (2026-08-19): MERGED, confirmed via GitHub MCP |
| 2026-08-20 | cross-host-conformance | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=0/DEEP) landed second, after #149/#150 (mesh-census coverage fix) merged first; this row is this session's independent, non-overlapping finding: `hook-conformance-both-hosts.test.mjs`'s Codex-side TEETH assertions were vacuous — every Codex hook (including `decision-gate`, the sole cross-host refusal gate) silently short-circuited through two undocumented exit branches before reaching real hook logic, so "no stderr/no artifacts/within timeout" passed by construction, never by measurement | #147 | #148 | yes | ACCEPT | Installed a real Stable-Spine test fixture; `decision-gate bash` 60ms/0B (stub) → 938ms/0B (real ALLOW), `session-start` 60ms/0B → 482ms/3112B real output. `test:integration` 38 files + 5 targeted unit files reproduced byte-identical baseline vs candidate; zero regressions | c75dce4c384fb8863d0e3566cd232b50e710613d4cb6a8abd9b1354618983d51 | #142/#143 (2026-08-19): MERGED — confirmed via GitHub MCP, not assumed |
| 2026-08-21 | brain-currency | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=1/DEEP) landed first on branch `dream/2026-08-21-brain-currency` (one commit, `93d0711`, fixing an unescaped-RegExp bug in `card-from-source.mjs`'s `insertSorted()` heading match — no issue/PR opened by that session as of this row); this row is this session's independent, non-overlapping finding: `scripts/brain-score.mjs`'s `readCoverage()` computed the `catalogue` coverage dimension from `storesAt(root)` without checking `fs.existsSync(root)` first, so a host whose local store cache was never materialized (this container: `stores 0 dark 0`) reported a real, `status:'current'` `0`/`"0/206 live repos"` — indistinguishable from genuine near-zero coverage. Same ambiguity `restore-local-ingests.mjs`'s `classify()` was fixed for in PR #143 (Night 1), never carried to this sibling reader. Pushed to a suffixed branch, `dream/2026-08-21-brain-currency-coverage-never-materialized`, to avoid colliding with the concurrent firing's branch | #154 | #155 | yes | ACCEPT | New TEETH test fails on pre-candidate code (`expected +0 to be null`), 16/16 pass post-fix; `test:integration` byte-identical failure set baseline vs candidate (6 files/11 tests, all pre-existing/environmental), diffed via `git stash`; blast radius confirmed closed by two independent greps (this session + an adversarial critic agent) — only the dimension's own 2 test files import it. Independent critic verdict CLEAR, with one noted non-blocking gap (ENOTDIR/EACCES not disambiguated, inherited unchanged from PR #143's precedent, not introduced tonight) | 931c60b0c2c0a33c1b2c73c350321b6f5db0c6b48b4f5814178b6133967627a8 | #142/#143 (2026-08-19): MERGED. #147/#148, #149/#150 (2026-08-20): both MERGED. All confirmed via GitHub MCP, not assumed |
| 2026-08-23 | grounding-quality | `kb/self-retrieval-bench.mjs` (this repo's cheap, always-available self-retrieval MRR gate) printed `mode=HYBRID` whenever `KB_HYBRID=1` was set, but `forge-ask.mjs`'s `searchKb()` — the function it benchmarks — has no `KB_HYBRID` gate at all: every run was dense-only regardless of the flag, a witness that cannot fail. The same drift had spread into 3 documentation sites: `kb/forge-hybrid.mjs`'s header comment, ADR-025's Open Items, and `docs/BRAIN-WRITE-READ-ARCHITECTURE.md` (the last found only by an independent critic's second pass) all claimed `mmrRerank` was a "ported function" not yet wired in, when it was never implemented at all — named only in that comment. `hybridScores`/`multiFieldBM25` are real exports but proven dead code (zero callers anywhere in the repo). Note for the owner: found but not this session's place to fix — a 2026-08-22 `enforcement-integrity` night (draft PRs #157/#159) has no corresponding ledger row; the "row on every run" invariant was missed that night | #161 | #162 (draft) | yes | ACCEPT | New TEETH tests fail on pre-candidate code (`forge-hybrid-port-claims`: `unbacked:['mmrRerank']`; `self-retrieval-bench-hybrid-mode`: crashes on import — no `isMain` guard existed), 8/8 pass post-fix, verified via `git stash` (tracked source only, tests kept). `test:unit` (278 files) and `test:integration` (38 files) byte-identical baseline vs candidate failure sets, diffed via `git stash` — all pre-existing (chmod/root-user, sqlite3, `@xenova/transformers`, headless-timing). `doc-currency --check`: 52→51 BLOCKING (this diff's own ADR-025 stamp-lag resolved, zero new violations). Independent critic first pass: BLOCKED (same false claims still live, unguarded, in `docs/BRAIN-WRITE-READ-ARCHITECTURE.md` — a live-status doc, not history like ADR-025's carve-out); fixed in commit `b180807`, same pattern. Non-blocking gaps recorded: detector fragile to a mid-segment fabricated claim; ADR-025's historical Decision/Env-knobs section not annotated for consistency (deliberately, per critic's own carve-out judgment) | 053067c332b5436b639b56ca49256e8d0a97e79494170f13442187936bc29750 | #142/#143 (2026-08-19): MERGED. #147/#148, #149/#150 (2026-08-20): MERGED. #154/#155 (2026-08-21): issue open, PR #155 CLOSED not merged (superseded by a concurrent firing's unlabeled branch). All confirmed via GitHub MCP, not assumed |
| 2026-08-23 | grounding-quality | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=3/DEEP) landed first as #161/#162 (HYBRID-mode-labeling fix in `kb/self-retrieval-bench.mjs`/`forge-hybrid.mjs`); this row is this session's independent, non-overlapping finding: `scripts/build-primer.mjs`'s own header comment claimed "Same proven shape as build-l2" but `fs.writeFileSync` ran unconditionally, one line before the citation-count check that only ever chose a console.log label — a primer citing zero real retrieved sources still shipped to the live, searchable `kb/<name>-primer.md`, where it is hard-boostable to rank #1 for "what is X" queries via `forge-ask.mjs`'s `targetPrimerSlug` force-route. `build-l2.mjs` already enforced the opposite (reject to `kb/l2/rejected/`); this was the fix a prior night (`59d15d6`, 2026-08-19) fully diagnosed and left as an all-`.todo` skeleton pending human sign-off. Pushed to a suffixed branch, `dream/2026-08-23-grounding-quality-primer-write-gate`, rebased onto the concurrent firing's tip so ledger rows append in order. Also corrects that firing's row: it claimed the 2026-08-22 night's ledger-row invariant was missed; independently verified both `dream/2026-08-22-enforcement-integrity*` branches DO carry a ledger row in their own diffs — the rows just haven't reached `main` because PRs #157/#159 are still open drafts, which is the documented by-design behavior, not a miss | #163 | #164 | yes | ACCEPT | Guard proven to fail first: an identical synthetic 0-ref primer, run through the exact pre-candidate write logic in isolation (no network calls), lands at the live path; through the candidate's `writeGated`, lands at `kb/rejected/`. 13/13 new/rewritten unit tests pass (prior file was 15 `it.todo`, 0 executing). `test:unit` (rebased onto concurrent firing's tip) and `test:integration`: byte-identical failure sets baseline vs candidate via `git stash` — same 4 unit files / 5 integration files both sides, all pre-existing (root-permission-simulation running as root; missing `@xenova/transformers`/sqlite3 native deps). `claims:verify`: 3 PASS/4 SKIP, identical. Independent critic verdict CLEAR, including empirical mutation-testing of `writeGated` (forced live-dir write, watched 2/13 tests go red, confirming the new guard is not vacuous) and line-by-line confirmation `build-l2.mjs`'s behavior is a pure, unchanged refactor | 86bdbbc2a5287ca12c4694b687450834b1d48d491b72fb92fcdb65ad7580749c | #142/#143 (2026-08-19): MERGED. #147/#148, #149/#150 (2026-08-20): both MERGED. #154/#155 (2026-08-21): #155 CLOSED not merged, integrated via a separate commit (`420854b`) instead. #156/#157, #158/#159 (2026-08-22): both still OPEN drafts — `windows-unit` failing on both, `integration` also failing on #157; not this session's PRs to drive, noted for the record. #161/#162 (2026-08-23, concurrent this same night): OPEN draft, filed moments before this row. All confirmed via GitHub MCP, not assumed |
