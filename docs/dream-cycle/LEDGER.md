| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-08-19 | memory-durability | `restore-local-ingests.mjs` could not distinguish "store root never materialized on this host" from "wiped" — a fresh checkout, CI runner, or this nightly agent's own container all logged an identical false-wipe alarm | #142 | #143 | yes | ACCEPT | 7 new unit tests, red→green; `test:integration` 37 files reproduced identically vs unmodified `main`; both-hosts conformance green. Baseline exit 1 / wipe-wording → candidate exit 2 / honest "NOT evidence of a wipe" wording, on this container's real state | 275223bea7234cd4ebef5182083ea627d9aba459828248bc9f1398f62787d019 | night one — ledger was empty, nothing to re-check |
| 2026-08-20 | cross-host-conformance | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=0/DEEP) landed first as #147/#148 (test-harness fix, vacuous Codex TEETH assertions); this row is this session's independent, non-overlapping finding: `hook-registry.mjs`'s own drift-detection mesh census never enumerated `plugin/hooks/codex-hooks.json`, so M1/M3/M5/M6 had zero automated coverage of the Codex host | #149 | #150 | yes | ACCEPT | Codex layer added, all 16 registrations resolve via the existing shim table (zero invented contracts), M1/M3/M5/M6 clean; 2 downstream consumer regressions found+fixed (`dispatchGateWiring()`, `checkCoexistence()`). `test:integration` candidate's 9 failures a strict subset of baseline's 10, byte-identical overlap | b3822a6584510d770d107a52884752399eb83c5352d05dff7dff8e7ffcfffe47 | #142/#143 (2026-08-19): MERGED, confirmed via GitHub MCP |
| 2026-08-20 | cross-host-conformance | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=0/DEEP) landed second, after #149/#150 (mesh-census coverage fix) merged first; this row is this session's independent, non-overlapping finding: `hook-conformance-both-hosts.test.mjs`'s Codex-side TEETH assertions were vacuous — every Codex hook (including `decision-gate`, the sole cross-host refusal gate) silently short-circuited through two undocumented exit branches before reaching real hook logic, so "no stderr/no artifacts/within timeout" passed by construction, never by measurement | #147 | #148 | yes | ACCEPT | Installed a real Stable-Spine test fixture; `decision-gate bash` 60ms/0B (stub) → 938ms/0B (real ALLOW), `session-start` 60ms/0B → 482ms/3112B real output. `test:integration` 38 files + 5 targeted unit files reproduced byte-identical baseline vs candidate; zero regressions | c75dce4c384fb8863d0e3566cd232b50e710613d4cb6a8abd9b1354618983d51 | #142/#143 (2026-08-19): MERGED — confirmed via GitHub MCP, not assumed |
| 2026-08-21 | brain-currency | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=1/DEEP) landed first on branch `dream/2026-08-21-brain-currency` (one commit, `93d0711`, fixing an unescaped-RegExp bug in `card-from-source.mjs`'s `insertSorted()` heading match — no issue/PR opened by that session as of this row); this row is this session's independent, non-overlapping finding: `scripts/brain-score.mjs`'s `readCoverage()` computed the `catalogue` coverage dimension from `storesAt(root)` without checking `fs.existsSync(root)` first, so a host whose local store cache was never materialized (this container: `stores 0 dark 0`) reported a real, `status:'current'` `0`/`"0/206 live repos"` — indistinguishable from genuine near-zero coverage. Same ambiguity `restore-local-ingests.mjs`'s `classify()` was fixed for in PR #143 (Night 1), never carried to this sibling reader. Pushed to a suffixed branch, `dream/2026-08-21-brain-currency-coverage-never-materialized`, to avoid colliding with the concurrent firing's branch | #154 | #155 | yes | ACCEPT | New TEETH test fails on pre-candidate code (`expected +0 to be null`), 16/16 pass post-fix; `test:integration` byte-identical failure set baseline vs candidate (6 files/11 tests, all pre-existing/environmental), diffed via `git stash`; blast radius confirmed closed by two independent greps (this session + an adversarial critic agent) — only the dimension's own 2 test files import it. Independent critic verdict CLEAR, with one noted non-blocking gap (ENOTDIR/EACCES not disambiguated, inherited unchanged from PR #143's precedent, not introduced tonight) | 931c60b0c2c0a33c1b2c73c350321b6f5db0c6b48b4f5814178b6133967627a8 | #142/#143 (2026-08-19): MERGED. #147/#148, #149/#150 (2026-08-20): both MERGED. All confirmed via GitHub MCP, not assumed |
| 2026-08-26 | brain-currency | `scripts/brain-stamp.mjs`'s `builtFromSha` was computed exclusively from the local clone's live `git rev-parse HEAD` at stamp time; it never read `kb/RVF-GENERATIONS.json`'s `sourceCommit`, recorded once at actual RVF-build time by `rvf-generation.mjs`'s `writeRvfGeneration()`. This is the exact "clone freshness is not artifact freshness" defect ADR-069's 2026-08-21 audit named, and it is live, not theoretical: this repo's own committed `kb/RVF-GENERATIONS.json` already disagrees with a live-HEAD read for `synthlang` (`sourceCommit` `69599563...`) and `autogenous` (`sourceCommit` `b5c6e838...`), ADR-069's own cited examples; 27/78 recorded stores have a non-null `sourceCommit` today, all previously invisible to the stamper. New `resolveBuiltFromSha()` (pure, in a new `scripts/brain-stamp-resolve.mjs` so it can be unit-tested without importing `brain-stamp.mjs`'s side-effecting top level — see Effect) prefers the recorded generation, case-insensitively, falling back to live HEAD only when no record exists. Currency-log row added to ADR-069 in the same change (it governs `scripts/brain-stamp.mjs`) | #175 | #176 | yes | ACCEPT | New TEETH test fails on pre-candidate code (module absent), 5/5 pass post-fix, including a check against this repo's own real `synthlang` generation record. This container has zero local `.rvf` files, so `resolveBuiltFromSha` is never invoked in a real end-to-end run here; ran the real CLI baseline vs candidate via `git stash` and diffed `data/manifest.json` byte-for-byte (timestamps excluded) — identical, 0 built/173 total both sides, proving zero regression on this container's actual path. `test:integration` byte-identical failure set baseline vs candidate (5 files/9 tests, all pre-existing/environmental: `sqlite3`/`@xenova/transformers`/headless-Chromium-path missing); both-hosts conformance gate unaffected. Independent critic verdict CLEAR. Caught and fixed, mid-session, an import-time side-effect hazard in the test's own first draft (importing `brain-stamp.mjs` directly silently restamped this checkout's real `data/manifest.json`/`primer/ruvnet-primer.md`; reverted via `git checkout`, worked around by extracting the pure function) — recorded in Security Review, not hidden | 7bf09af3867bbc1c8fcee6c7fdd33b68fbc548545481dfcdc9bf2ab2df188ad5 | #142/#143 (08-19), #147/#148, #149/#150 (08-20): all MERGED. #154/#155 (08-21): PR closed unmerged, but the change landed directly on `main` via commit `420854b` — confirmed via GitHub MCP, not assumed. Separately: every dream-cycle PR opened since 2026-08-22 (#157, #159, #162, #164, #167, #168, #172, #174 — 8 total) is still open and draft, zero merged as of tonight — a review backlog worth the owner's attention, flagged in issue #175, independent of tonight's candidate |
| 2026-08-26 | brain-currency | CONCURRENT NIGHT — a separate firing of this same routine (same SLOT=1/DEEP) landed first as #175/#176 (`brain-stamp.mjs`'s `builtFromSha` provenance fix, and pushed this date's first ledger row directly to `main` as `3124b54` ahead of any candidate PR merging — a deviation from this repo's own "ledger row ships in the candidate PR" convention, noted here rather than silently followed); this row is this session's independent, non-overlapping finding: `scripts/brain-score.mjs`'s `readCoverage()` and `scripts/restore-local-ingests.mjs`'s `classify()` both used `fs.existsSync(root)` to detect a never-materialized store root — `true` not only for a real directory but also for a stray FILE at that path (`ENOTDIR`) or an unreadable directory (`EACCES`), letting `storesAt()`/`cardsAt()`'s swallowed-exception `[]` render as a false current `0` or a false `WIPED` alarm for those two errno classes, the exact gap PR #143/#155's own commit messages flagged as an accepted, unfixed fast-follow after fixing the `ENOENT` case | #177 | #178 | yes | ACCEPT | New `rootNeverMaterialized()` (`kb/store-root.mjs`) TEETH-tested: reverting only the 2 call-site changes reproduces `expected +0 to be null` and `expected 'WIPED' to be 'NEVER-MATERIALIZED'`; 42/42 pass restored. `test:integration` byte-identical baseline vs candidate (5 files/9 tests of 313, all pre-existing/environmental). Full `test:unit` ran to completion (386s): 4/277 files failed, all pre-existing `chmod`-based EACCES fixtures that don't enforce under this container's root user, none touching the changed files; 3403/3592 tests passed | 2ba7d4ca1a95eccf0d18f4dba8c44495def84903256350985b2a552f416be240 | #142/#143 (08-19), #147/#148, #149/#150 (08-20): MERGED. #154/#155 (08-21): PR closed unmerged, change landed on `main` via `420854b`. #175/#176 (08-26, concurrent): open draft as of this row. Separately: every dream-cycle PR opened since 2026-08-22 (#157, #159, #162, #164, #167, #168, #172, #174 — 8 total) is still open/draft, zero merged since 2026-08-20/21 — a review backlog worth the owner's attention, flagged in #175 and again here |
| 2026-08-30 | cross-host-conformance | Issue #134's fix (`RUVNET_BRAIN_PROJECT_DIR \|\| cwd`, shared by `learn-capture.sh`'s writer and `learn-flush.mjs`/`health-repair.mjs`'s readers) never fires in production on either host — grep confirms `RUVNET_BRAIN_PROJECT_DIR`'s only non-test writer is `health-repair.mjs`'s own self-referential passthrough; `hook-shim.mjs` forwards `process.env` unmodified and `codex-hook-adapter.mjs` builds its own env block without it — so a drifted cwd mid-session still orphans learning-queue events on both hosts, exactly as #104 did before #134, while the test suite stayed green only by manually injecting the never-actually-set variable | #193 | #194 | yes | ACCEPT | Reused `project-identity.mjs`'s `projectDirectory()` (containment-checked, from #85/#107) in the two Node readers plus an equivalent bash containment check in the writer, rather than an unconditional `CLAUDE_PROJECT_DIR` fallback — an unconditional first draft was caught and revised mid-session (see PR Security Review) because it would have reopened #85/#107's bug class. New TEETH case fails on `main` (`expected [] to not deeply equal []`, queue orphaned), 4/4 pass post-fix incl. a new containment-negative case; 13-file targeted regression sweep 204/210 pass (1 pre-existing chmod/root-user failure, confirmed against clean `main`); `test:integration` byte-identical baseline vs candidate (5 files/9 tests of 313, all pre-existing/environmental) | 9d5ae42dc17cef1f2d5d6c12956aeb2a23d45d8160c490ff0d7c76d261560938 | #142/#143 (08-19), #147/#148, #149/#150 (08-20), #175/#176 (08-26, since merged/landed — recheck at write time via GitHub MCP, not assumed stale): all resolved. #154/#155 (08-21): closed unmerged, landed via `420854b`. #177/#178 (08-26): closed unmerged, status not reverified tonight. SEPARATELY: 14 dream-cycle draft PRs open as of tonight (#157,#159,#162,#164,#167,#168,#172,#174,#176,#182,#184,#186,#188,#190), zero merged since #150/#148 (2026-08-20) — 10 days, a review-capacity backlog worth the owner's direct attention, independent of tonight's candidate. ALSO: this session's own STEP 0.5 credentials check leaked `OPENROUTER_API_KEY` into its transcript via a bash quoting mistake — the SECOND occurrence of this exact mistake (first: 2026-08-26's ledger row/report) — reported to the owner directly both times; recommend a standing fix to the routine itself, not another per-night self-report. Independent critic (separate agent, verified by direct execution against edge cases, not just reading) verdict CLEAR: containment logic holds for substring-not-prefix/exact-match/unset/trailing-slash cases; two theoretical non-blocking gaps noted (a doubled trailing slash, a symlink hop in cwd) both degrade toward the already-accepted pre-fix orphaning behavior, never toward a wrong-directory write. Also found, out of scope for tonight and filed separately: `plugin/mcp/managed-cli-interface.mjs` has the identical unfixed pattern for agentic-flow/qe routing — issue #195 |
