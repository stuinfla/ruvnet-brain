name: canonical-qa

on:
  pull_request:
    branches: [main]
  workflow_dispatch: {}
  workflow_call:
    inputs:
      candidate_sha:
        description: Exact candidate SHA owned by the calling QA workflow
        required: true
        type: string

concurrency:
  group: ruvnet-brain-canonical-qa-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  canonical-qa:
    name: canonical-qa
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.candidate_sha || github.sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci --no-audit --no-fund --ignore-scripts
      - name: Generate convergence manifest for the exact checked-out candidate
        # Pull requests run on a synthetic merge ref, whose content identity differs from the
        # branch tip. Generate the manifest inside that exact checkout so the freshness gate never
        # consumes a branch-stale Hubble/convergence snapshot.
        run: npm run convergence:write
      - name: Run the bounded exact-SHA QA contract
        env:
          QA_TIMEOUT_MS: 900000
        run: npm run qa:pr
